<%= form_for @oval, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :as2, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :as2, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                ovals_path, :class => 'btn' %>
  </div>
<% end %>
